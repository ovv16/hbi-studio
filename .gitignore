# Raw client source photos — originals, not needed to serve the site,
# and they are identifiable client material. Keep them local.
photo do i posle/
фото для оброботки/

# Local Claude Code permissions — machine-specific
.claude/settings.local.json

# Local secrets — never commit
site/config.local.js
*.env
.env*

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
